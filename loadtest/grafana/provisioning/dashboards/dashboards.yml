apiVersion: 1

providers:
  - name: k6 queue dashboards
    orgId: 1
    folder: Load Tests
    type: file
    disableDeletion: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
